import { Home } from '@/components/home'
import { Header } from '@/components/layout/header'

export const HomePage = () => {
    return (
        <>
            <Header title='home' />
            <Home />
        </>
    )
}
