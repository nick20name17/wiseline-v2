import { capitalize } from './capitalize'
import { getUpperCaseInitials } from './get-uppercase-initials'
import { isErrorWithMessage } from './is-error-with-message'
import { stopEvent, stopPropagation } from './stop-events'
import { trunc } from './trunc'
import { getQueryParamString } from '@/utils/get-query-param-string'

export {
    capitalize,
    getQueryParamString,
    getUpperCaseInitials,
    isErrorWithMessage,
    stopEvent,
    stopPropagation,
    trunc
}
